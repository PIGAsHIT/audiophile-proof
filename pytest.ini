[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    external: marks tests as external API calls (requires network)
    smoke: quick health check tests
